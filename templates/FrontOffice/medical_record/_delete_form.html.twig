<form method="post" action="{{ path('app_medical_record_delete', {'id': medical_record.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medical_record.id) }}">
    <button class="btn">Delete</button>
</form>
