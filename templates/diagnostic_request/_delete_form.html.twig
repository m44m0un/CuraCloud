<form method="post" action="{{ path('app_diagnostic_request_delete', {'id': diagnostic_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ diagnostic_request.id) }}">
    <button class="btn">Delete</button>
</form>




