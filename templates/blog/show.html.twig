{#{% extends 'base.html.twig' %}#}
{% extends 'showBlog.html.twig' %}

{% block title %}Blog{% endblock %}

{# {% block body %}
    <h1>Blog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ blog.content }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ blog.subject }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ blog.image }}</td>
            </tr>
            <tr>
                <th>SubmissionDate</th>
                <td>{{ blog.submissionDate ? blog.submissionDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blog_index') }}">back to list</a>

    <a href="{{ path('app_blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %} #}

{% block body %}

   <div class="container">
            <div class="row">
              <div class="col-sm-8">
                <div class="post">


                <div class="post-thumbnail"><a href="#"><img src="{{ blog.image }}" alt="Blog Image"></a></div>
                  <div class="post-header font-alt">
                    <h1 class="post-title"><a href="{{ path('app_blog_show', {'id': blog.id}) }}" class="post-link">{{ blog.title }}</a></h1>
                    {# <div class="post-meta">By&nbsp;<a href="#">Mark Stone</a>| 23 November | 3 Comments | <a href="#">Photography, </a><a href="#">Web Design</a> #}
                     <div class="post-meta"><a href="#">{{ blog.subject }}</a>
                    </div>
                  </div>
                    <div class="post-entry">
                        <p>{{ blog.content }}</p>
                    </div>
                    <div >
                        <button class="btn btn-primary btn-circle"><a href="{{ path('app_blog_index') }}">Back to Blog List </a></button>
                        <button class="btn btn-primary btn-circle"><a href="{{ path('app_blog_edit', {'id': blog.id}) }}">Edit </a></button>
                        {# <button class="btn btn-primary btn-circle" type="button">Submit</button> #}
                         {{ include('blog/_delete_form.html.twig') }}
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
