{% extends 'editBlog.html.twig' %}

{% block title %}Edit Blog{% endblock %}

{% block body %}

     <section class="module bg-dark-30 about-page-header" data-background="{{ blog.image }}">
          <div class="container">
            <div class="row">
              <div class="col-sm-6 col-sm-offset-3">
                <h1 class="module-title font-alt mb-0">Forms</h1>
              </div>
            </div>
          </div>
        </section>
        <div class="container">
            <div class="row">
              <div class="col-sm-8 col-sm-offset-2">
     <h4 class="font-alt mb-0">Edit Blog</h4>
     <hr class="divider-w mt-10 mb-20">

    {{ include('blog/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn btn-primary btn-circle"><a href="{{ path('app_blog_index') }}">Back to Blog List </a></button>

    {{ include('blog/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %} 


{# {% extends 'editBlog.html.twig' %} #}

{# {% block title %}Edit Blog{% endblock %}

{% block body %}
    <section class="module bg-dark-30 about-page-header" data-background="assets/images/about_bg.jpg">
        <div class="container">
            <div class="row">
                <div class="col-sm-6 col-sm-offset-3">
                    <h1 class="module-title font-alt mb-0">Edit Blog</h1>
                </div>
            </div>
        </div>
    </section>
    <section class="module">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 col-sm-offset-2">
                    <h4 class="font-alt mb-0">Edit Form</h4>
                    <hr class="divider-w mt-10 mb-20">

                    {{ include('blog/_form.html.twig', {'button_label': 'Update'}) }}

                    <a href="{{ path('app_blog_index') }}" class="btn btn-default">Back to List</a>

                    {{ include('blog/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>
{% endblock %} #}